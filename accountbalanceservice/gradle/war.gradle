apply plugin: "war"

bootWar {
    mainClass = "com.kode.accountbalanceservice.AccountbalanceserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
