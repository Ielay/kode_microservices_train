apply plugin: "war"

bootWar {
    mainClass = "com.kode.accountinfoservice.AccountinfoserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
